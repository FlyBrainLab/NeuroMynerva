/**
 * Exports only the mesh required to render larva NLP.
 */
export declare var LarvaMesh: {
    AL: {
        filename: string;
        label: string;
        highlight: boolean;
        background: boolean;
        color: number[];
    };
    MB: {
        filename: string;
        label: string;
        highlight: boolean;
        background: boolean;
        color: number[];
    };
    CNS: {
        filename: string;
        label: string;
        highlight: boolean;
        background: boolean;
        color: number[];
    };
};
